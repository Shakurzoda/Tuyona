import styles from './Filtres.module.css';

const Filtres = () => {
    return (
        <div>
            <h1>
                Фильтры
            </h1>
        </div>
    );
};

export default Filtres;